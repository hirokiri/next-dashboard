import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Customers',
};

export default function Customers({ }) {
    return (
        <>
            <h1>Unko----!</h1>
            <p>Customers Page</p>
        </>
    )
}